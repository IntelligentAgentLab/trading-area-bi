import streamlit as st

st.set_page_config(page_title="소비상권 BI 데모", page_icon="📊", layout="wide")

st.title("소비·상권·유통 BI 프로젝트 — 더미 데이터 데모")

st.markdown(
    """
    왼쪽 사이드바에서 페이지를 선택하세요. 전부 **더미(가짜) 데이터**로 만든
    화면 구조 데모입니다.

    - **대시보드**: 매출 추이 · 히트맵 · 상권 지도 · KPI 카드 (B 담당 영역)
    - **토픽모델링**: 토픽 비중 시계열 · 워드클라우드 (C 담당 영역)
    - **인과추론**: DiD 평행추세 · 처치효과 추정 (D 담당 영역)
    - **통합요약**: 세 페이지의 핵심 결과를 업종 축으로 한 화면에 조합 (현상 → 원인 → 효과)

    실제 프로젝트에서는 A(데이터 엔지니어링)가 만든 패널 데이터를
    B·C·D가 각자 페이지에 연결하는 구조가 됩니다.

    **공통 필터**: 사이드바의 "업종" 선택은 `common.py`의 `render_filters()`를 통해
    모든 페이지가 `st.session_state`로 공유합니다 — 한 번 고르면 대시보드·토픽모델링·
    통합요약 페이지 전체에서 같은 업종을 계속 보게 됩니다.
    """
)

st.info("좌측 사이드바 'pages' 메뉴에서 각 화면으로 이동하세요.")
